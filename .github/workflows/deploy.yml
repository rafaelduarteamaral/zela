name: Deploy to Cloudflare

on:
  push:
    branches:
      - main
  workflow_dispatch:

env:
  NODE_ENV: production
  CF_API_TOKEN: ${{ secrets.CF_API_TOKEN }}
  CF_ACCOUNT_ID: ${{ secrets.CF_ACCOUNT_ID }}
  CF_PAGES_PROJECT_FRONTEND: ${{ secrets.CF_PAGES_PROJECT_FRONTEND || 'financezap-frontend' }}
  CF_PAGES_PROJECT_LANDING: ${{ secrets.CF_PAGES_PROJECT_LANDING || 'zela-landing' }}

jobs:
  deploy:
    name: Build & publish frontend/backend
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Backend | Install dependencies
        run: |
          cd financezap-backend
          npm ci

      - name: Backend | Run tests
        run: |
          cd financezap-backend
          npm run test:ci

      - name: Backend | Build worker
        run: |
          cd financezap-backend
          npm run build:skip-tests

      - name: Backend | Deploy Cloudflare Worker
        run: |
          cd financezap-backend
          npm run deploy:worker

      - name: Frontend | Install dependencies
        run: |
          cd financezap-frontend
          npm ci

      - name: Frontend | Build
        run: |
          cd financezap-frontend
          npm run build

      - name: Frontend | Deploy Cloudflare Pages
        run: |
          cd financezap-frontend
          npx wrangler pages deploy dist --project-name "${{ env.CF_PAGES_PROJECT_FRONTEND }}"

      - name: Landing | Install dependencies
        run: |
          cd financezap-landing
          npm ci

      - name: Landing | Build
        run: |
          cd financezap-landing
          npm run build

      - name: Landing | Deploy Cloudflare Pages
        run: |
          cd financezap-landing
          npx wrangler pages deploy dist --project-name "${{ env.CF_PAGES_PROJECT_LANDING }}"
